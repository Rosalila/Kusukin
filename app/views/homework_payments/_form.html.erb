<%= form_for(@homework_payment) do |f| %>
  <% if @homework_payment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@homework_payment.errors.count, "error") %> prohibited this homework_payment from being saved:</h2>

      <ul>
      <% @homework_payment.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.text_field :user_id %>
  </div>
  <div class="field">
    <%= f.label :homework_id %><br>
    <%= f.text_field :homework_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
